import 'package:flutter/material.dart';

import 'circular_fab_menu_widget.dart';

class CircularMenuPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: CircularFABMenuWidget(),
      ),
    );
  }
}
