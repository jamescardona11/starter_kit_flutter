import 'package:flutter/material.dart';

class SharedPreferencesPage extends StatelessWidget {
  /// default constructor
  const SharedPreferencesPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('No release yet'),
      ),
    );
  }
}
